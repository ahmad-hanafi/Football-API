export const SETAREAS = (state, areas) => {
    state.areas = areas
}

export const SET_AREA_BY_ID = (state, areasById) => {
    state.areasById = areasById
}

export const SET_TEAMS = (state, teams) => {
    state.teams = teams
}

export const SET_TEAM = (state, team) => {
    state.team = team
}

export const SET_PLAYER = (state, player) => {
    state.player = player
}

export const SET_NUMBER_OF_PLAYER = (state, number) => {
    state.number = number
}
